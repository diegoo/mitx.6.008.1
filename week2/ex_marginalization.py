#!/usr/bin/python3
# -*- coding: utf-8 -*-

from fractions import *

X = {'sunny': float(Fraction(1,4) + Fraction(1,4)), 'rainy': float(Fraction(1,12) + Fraction(1,12)), 'snowy': float(Fraction(1,6) + Fraction(1,6))}
print(X)

Y = {1: float(Fraction(1,4) + Fraction(1,12) + Fraction(1,6)), 0: float(Fraction(1,4) + Fraction(1,12) + Fraction(1,6))}
print(Y)
